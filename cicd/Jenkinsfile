pipeline {
    agent none  // No agent is used in the pipeline
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    // Build the Docker image and tag it with 'latest'
                    sh 'docker build -t my-python-agent:latest .'
                }
            }
        }
    }
}
